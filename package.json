{
  "name": "dnd-battle-manager",
  "version": "1.0.0",
  "description": "",
  "main": "dist/Undos.js",
  "scripts": {
    "build": "npm run clean && babel src --out-dir dist",
    "clean": "rm -rf dist",
    "dev": "npm run build && node-debug .",
    "start": "npm run build && node .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --max-warnings 0 --fix && echo 'no errors' || echo 'finished with errors'"
  },
  "author": "John Cronk <john.a.cronk@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-eslint": "^7.2.3",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-1": "^6.24.1",
    "eslint": "^4.5.0",
    "eslint-plugin-babel": "^4.1.2"
  },
  "dependencies": {
    "readline-sync": "^1.4.7"
  }
}
